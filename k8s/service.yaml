apiVersion: v1
kind: Service
metadata:
  name: mlops-face-app-service
spec:
  type: NodePort
  selector:
    app: mlops-face-app
  ports:
    - protocol: TCP
      port: 5001
      targetPort: 5001
      nodePort: 30080